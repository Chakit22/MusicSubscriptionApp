export type Song = {
  title: string;
  album: string;
  artist: string;
  image_url: string;
  year: string;
};

export type Subscription = {
  user_email: string;
  title: string;
  album: string;
  artist: string;
};
